import { LevelLabelPipe } from './level-label-pipe';

describe('LevelLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
